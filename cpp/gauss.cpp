void gauss(v1, v2) { //check datatypes
	double x1, x1, z v1, v2, ran1, sd;
	z = 2.0;
	while(z > 1.0) {
		x1 = 2.0*ran1(zzzz) - 1; //better names please?
		x2 = 2.0*ran1(zzzz) - 1;
		z = x1*x1 + x2*x2;
	}
	z = sqrt(-2.0*log(z)/z); //check which log to be used?
	v1 = x1*z*sqrt(kbt/mass_fl); //gaussian with variance kbt/m and mean zero
	v2 = x2*z*sqrt(kbt/mass_fl);
}

